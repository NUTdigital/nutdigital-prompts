# Changelog

All notable changes to the prompt spec are documented here.
Versioning follows the `schema_version` field inside the JSON itself.

## [1.0] - 2026-07-29

### Added
- Initial spec: fidelity/overlay rules, palette, line weights
- Wheel motion logic (hub / mid / outer zone + motion trail)
- Shadow, track, background constraints
- Global prohibit list + render_checks
